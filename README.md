# XLibre Xserver → V Language Translation This repository contains a helper script, [`translate_xlibre.sh`](./translate_xlibre.sh), for batch‑translating the **XLibre Xserver** C source tree into [V language](https://vlang.io/) source files. It is designed for **safe, reproducible** translation runs: - Preserves the original directory structure in the output folder - Avoids accidental `/home/...` nesting bugs - Creates `.err` logs for any file that fails to translate --- ## 📦 Requirements - **V compiler** with `translate c` support [Install instructions](https://github.com/vlang/v#installing-v) - **Bash** (tested on GNU Bash 5.x) - **GNU coreutils** (`realpath`, `mkdir`, etc.) - **findutils** (`find` with `-printf` support) --- ## 🚀 Usage ```bash ./translate_xlibre.sh /path/to/v /path/to/xlibre /path/to/output update this readme with full context
